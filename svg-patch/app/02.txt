<g id="svgMapScene"><g id="svgGraticule"></g><g id="svgCountries"></g><g id="svgPoints"></g></g>
          <g id="svgLabels"></g>
        </svg>
        <div class="map-zoom-controls" aria-label="Zoom controls">
          <button id="svgZoomIn" type="button" aria-label="Zoom in">+</button>
          <button id="svgZoomOut" type="button" aria-label="Zoom out">−</button>
        </div>
        <div id="svgTooltip" class="map-tooltip is-hidden" role="tooltip"></div>
        <div id="svgSelection" class="map-selection is-hidden" aria-live="polite">
          <button id="svgSelectionClose" class="map-selection-close" type="button" aria-label="Close map selection">×</button>
          <div><strong id="svgSelectionName">Country</strong><span id="svgSelectionMeta">Use this as your next guess</span></div>
          <button id="svgSelectionUse" class="map-selection-use" type="button">Use as guess</button>
        </div>
        <div class="map-scale-hint" aria-hidden="true">Country borders · drag and zoom</div>
      </div>
    </div>`;

  const elements = {
    world: document.getElementById("svgWorldMap"),
    svg: document.getElementById("svgMapSvg"),
    scene: document.getElementById("svgMapScene"),
    graticule: document.getElementById("svgGraticule"),
    countries: document.getElementById("svgCountries"),
    points: document.getElementById("svgPoints"),
    labels: document.getElementById("svgLabels"),
    status: document.getElementById("svgMapStatus"),
    focus: document.getElementById("svgMapFocus"),
    reset: document.getElementById("svgMapReset"),
    expand: document.getElementById("svgMapExpand"),
    zoomIn: document.getElementById("svgZoomIn"),
    zoomOut: document.getElementById("svgZoomOut"),
    tooltip: document.getElementById("svgTooltip"),
    selection: document.getElementById("svgSelection"),
    selectionName: document.getElementById("svgSelectionName"),
    selectionMeta: document.getElementById("svgSelectionMeta"),
    selectionUse: document.getElementById("svgSelectionUse"),
    selectionClose: document.getElementById("svgSelectionClose")
  };

  function localDateKey(date = new Date()) {
    const year = date.getFullYear();
    const month = String(date.getMonth() + 1).padStart(2, "0");
    const day = String(date.getDate()).padStart(2, "0");
    return `${year}-${month}-${day}`;
  }

  function getPreferences() {
    try { return JSON.parse(localStorage.getItem(STORAGE_KEY) || "null") || {}; }
    catch { return {}; }
  }

  function currentState() {
    const preferences = getPreferences();
    const mode = document.querySelector(".mode-button.is-active")?.dataset.mode || preferences.mode || "daily";
    if (mode === "random") return preferences.randomGame || { mode, guesse